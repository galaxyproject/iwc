export default defineAppConfig({
    ui: {
        // primary: "amber",
        // This is a lot... amber is better?  Keep iterating towards galaxy-ish.
        primary: "gold",
        gray: "ebony-clay",
    },
});
